rootProject.name = "composite-forge"

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        google()
        maven("https://maven.minecraftforge.net/")
        maven("https://maven.fabricmc.net/")
    }
}